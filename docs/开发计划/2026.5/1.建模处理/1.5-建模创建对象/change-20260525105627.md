# 变更记录 - 1.5 建模创建对象

**时间**: 2026-05-25 10:56:27
**任务**: 1.5 建模创建对象
**版本**: 2026.5

## 原始功能文档备份

[backup-20260525105627.md](./backup-20260525105627.md)

## 变更内容

### 1. IBaseModel 接口方法签名变更

**变更前**:
```csharp
object CreateInstance(object?[] args)
```

**变更后**:
```csharp
object CreateInstance(params object?[] args)
```

### 2. 使用示例更新

所有 `CreateInstance()` 调用方式从显式传递数组改为可选参数方式：

**变更前**:
```csharp
var user = (User)model.CreateInstance(Array.Empty<object?>());
var order = (Order)model.CreateInstance(new object?[] { "order-123" });
```

**变更后**:
```csharp
var user = (User)model.CreateInstance();
var order = (Order)model.CreateInstance("order-123");
```

### 3. 源生成实现代码更新

所有 `CreateInstance()` 实现代码的签名从 `object?[] args` 改为 `params object?[] args`

## 变更原因

根据需求补充文档的**第三次返工**要求：
- 将 `CreateInstance(object?[] args)` 函数改为 `CreateInstance(param object?[] args)`
- 参数改为可选参数，使调用更加简洁

## 可能产生的影响

1. **源生成器代码**：需要更新所有生成 `CreateInstance()` 方法的源生成器
   - ModelableSourceGenerator
   - ModelTableSourceGenerator

2. **现有模型类实现**：需要更新所有手动实现 `CreateInstance()` 的模型类
   - Models 目录下的各模型类

3. **Demo 代码**：需要更新所有调用 `CreateInstance()` 的示例代码

4. **单元测试**：需要更新所有测试 `CreateInstance()` 的测试用例

5. **向后兼容性**：变更完全向后兼容，旧代码的数组调用方式仍然有效

## 验证建议

1. 验证无参调用 `CreateInstance()` 是否正常工作
2. 验证多参数调用 `CreateInstance(arg1, arg2, arg3)` 是否正常工作
3. 验证显式数组调用 `CreateInstance(new object?[] { arg1, arg2 })` 是否仍然有效
4. 验证 AOT 编译兼容性