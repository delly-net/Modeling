# 1.12 建模对象添加属性

## 任务信息

- **任务号**: 1.12
- **任务名称**: 建模对象添加属性
- **责任人**: 郑琳
- **任务类型**: 代码修改
- **任务描述**: 在 `IBaseModel` 中添加 `bool IsValue` 属性，基础值对象为true（含string），一般class对象为false

## 功能概述

在 `IBaseModel` 接口中新增 `IsValue` 属性，用于区分基础值对象和一般对象类型。

**IsValue 属性语义**：`IsValue` 属性用于区分基础值类型和一般对象类型。

此属性主要用于建模系统运行时判断模型类型，便于在类型转换、值处理等场景中区分值类型和引用类型。

## 技术背景

### 现有实现

**IBaseModel 接口**（当前定义）：

```csharp
/// <summary>
/// 模型对象接口，提供属性访问能力
/// </summary>
public interface IBaseModel
{
    /// <summary>
    /// 名称
    /// </summary>
    string Name { get; }

    /// <summary>
    /// 类型全名称
    /// </summary>
    string Namespace { get; }

    /// <summary>
    /// 创建模型类型的新实例
    /// </summary>
    /// <param name="args">构造函数参数数组</param>
    /// <returns>模型类型的新实例</returns>
    object CreateInstance(params object[] args);

    /// <summary>
    /// 模型类型信息，源生成阶段使用 typeof(T) 赋值
    /// </summary>
    Type ClassType { get; }

    /// <summary>
    /// 将输入对象解析为目标类型实例
    /// </summary>
    /// <param name="obj">输入对象</param>
    /// <returns>目标类型实例</returns>
    /// <exception cref="ArgumentException">当对象无法转换为目标类型时抛出</exception>
#if NETSTANDARD2_0
    object Parse(object obj);
#else
    object Parse(object? obj);
#endif

    /// <summary>
    /// 尝试将输入对象解析为目标类型实例
    /// </summary>
    /// <param name="obj">输入对象</param>
    /// <returns>目标类型实例，解析失败时返回 null</returns>
#if NETSTANDARD2_0
    object TryParse(object obj);
#else
    object? TryParse(object? obj);
#endif
}
```

**Models 目录中的基础值类型**：

- `StringModel` - string 类型
- `Int32Model` - int 类型
- `Int64Model` - long 类型
- `BooleanModel` - bool 类型
- `DoubleModel` - double 类型
- `DecimalModel` - decimal 类型
- `DateTimeModel` - DateTime 类型
- `GuidModel` - Guid 类型

这些基础值类型的 `IsValue` 应返回 `true`。

### 新增需求

**新增属性**：

```csharp
/// <summary>
/// 是否为值类型对象，基础值类型和 string 为 true，其他 class 类型为 false
/// </summary>
bool IsValue { get; }
```

**值类型判断规则**：

| 类型 | IsValue |
|:-----|:--------:|
| string | true |
| int / Int32 | true |
| long / Int64 | true |
| bool / Boolean | true |
| double / Double | true |
| decimal / Decimal | true |
| DateTime | true |
| Guid | true |
| enum | true |
| struct | true |
| Nullable<T> | true |
| 泛型类型（如 List<T>） | false |
| 其他 class 类型 | false |

## 实现方案

### 1. 修改接口定义

在 `IBaseModel.cs` 中新增 `IsValue` 属性：

```csharp
/// <summary>
/// 是否为值类型对象，基础值类型和 string 为 true，其他 class 类型为 false
/// </summary>
bool IsValue { get; }
```

### 2. 修改 Models 实现

在 `Models/` 目录下的所有 Model 类中实现 `IsValue` 属性（返回 `true`）：

```csharp
// StringModel.cs, Int32Model.cs, Int64Model.cs, BooleanModel.cs,
// DoubleModel.cs, DecimalModel.cs, DateTimeModel.cs, GuidModel.cs

/// <summary>
/// 是否为值类型对象
/// </summary>
public bool IsValue => true;
```

### 3. 修改源生成器

在 `ModelableSourceGenerator` 中为生成的模型类添加 `IsValue` 属性（返回 `false`）：

```csharp
// 对于 [Modelable] 标记的一般 class 类型
/// <summary>
/// 是否为值类型对象
/// </summary>
public bool IsValue => false;
```

### 4. 源生成器代码模板（条件编译支持）

生成的代码应使用条件编译区分不同 .NET 版本：

```csharp
#if !NETSTANDARD2_0
#nullable enable
#endif

namespace Delly.Modeling.Generated
{
    public sealed class emc__{ClassName} : IBaseModel
    {
        // ... 其他成员 ...

        /// <summary>
        /// 是否为值类型对象
        /// </summary>
        public bool IsValue => false;
    }
}
```

## 使用示例

```csharp
// 基础值类型
var stringModel = Model.Get<string>();
Console.WriteLine($"string.IsValue: {stringModel.IsValue}");  // true

var intModel = Model.Get<int>();
Console.WriteLine($"int.IsValue: {intModel.IsValue}");  // true

// 一般 class 类型
[Modelable]
public partial class User
{
    public string Id { get; set; } = string.Empty;
    public string Name { get; set; } = string.Empty;
}

var userModel = User.GetModel();
Console.WriteLine($"User.IsValue: {userModel.IsValue}");  // false

// 运行时判断
if (model.IsValue)
{
    // 值类型处理逻辑
    var parsedValue = model.TryParse(input);
}
else
{
    // 引用类型处理逻辑
    var instance = model.CreateInstance();
}
```

## 实现步骤

### 开发任务清单

- [ ] 在 `IBaseModel.cs` 接口中添加 `IsValue` 属性定义
- [ ] 修改 `StringModel.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `Int32Model.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `Int64Model.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `BooleanModel.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `DoubleModel.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `DecimalModel.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `DateTimeModel.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `GuidModel.cs` 实现 `IsValue` 属性（返回 `true`）
- [ ] 修改 `ModelableSourceGenerator` 为生成的类添加 `IsValue` 属性（返回 `false`）
- [ ] 在 Deme 项目中添加示例代码验证功能
- [ ] 添加单元测试覆盖所有 Model 类型
- [ ] 验证生成的代码编译通过，无警告

## 参考文档

- [IBaseModel.cs](../../../Delly.Modeling/IBaseModel.cs) - 需要修改的接口
- [Models/](../../../Delly.Modeling/Models/) - 需要修改的 Model 实现类
- [ModelableSourceGenerator.cs](../../../Delly.Modeling.Generator/ModelableSourceGenerator.cs) - 需要修改的源生成器
- [代码规范](../../../开发指南/代码规范.md) - XML 文档注释规范
- [注意事项](../../../开发指南/注意事项.md) - 开发注意事项

## 验收标准

1. `IBaseModel` 接口包含 `IsValue` 属性定义，且具有完整的 XML 文档注释
2. 所有 `Models/` 目录下的 Model 类实现 `IsValue` 属性并返回 `true`
3. 源生成器生成的类包含 `IsValue` 属性并返回 `false`
4. 生成的代码使用条件编译支持不同 .NET 版本
5. 生成的代码编译通过，无警告
6. 单元测试覆盖率 ≥ 80%
7. Deme 项目中的示例能正常运行
