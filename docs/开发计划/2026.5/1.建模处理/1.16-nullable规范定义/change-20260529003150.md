# 1.16 功能文档变更记录

**生成时间**: 2026-05-29 00:31:50

## 原始文档备份

[备份文件](./backup-20260529003150.rd)

## 变更内容

### 差异点：规范要求新增内容

**位置**: `1.16-需求补充.md` 第28行

**新增内容**:
```
- 多框架项目配置 `<Nullable>enable</Nullable>` 时需要加上指定框架兼容性适配条件
```

**说明**: memory.rd 中没有此内容，这是新增的规范要求。

**影响分析**:
此新增要求提出"多框架项目配置 `<Nullable>enable</Nullable>` 时需要加上指定框架兼容性适配条件"，但未说明具体的"框架兼容性适配条件"是什么。

**可能的实现方式**:
1. 使用条件编译区分不同目标框架的 nullable 配置（如 `$(TargetFramework)` 条件）
2. 使用 MSBuild 属性和条件来控制 nullable 行为
3. 其他方式

## 变更原因

本次变更基于任务执行过程中新增的需求补充，主要是：
1. 新增规范：多框架项目配置 nullable 时需要加上框架兼容性适配条件
2. 需要进一步澄清"框架兼容性适配条件"的具体实现方式

## 可能产生的影响

1. **功能文档更新**: 需要明确多框架项目配置 nullable 的具体实现方式
2. **实现步骤调整**: 可能需要在 csproj 中添加额外的条件编译或配置
3. **待确认清单**: 新增待确认项，需要明确框架兼容性适配条件的实现方式

## 待确认清单

根据需求补充文件，本次变更后的待确认清单：

### 已确认项

- [x] .NET Standard 2.0 目标框架编译器会忽略 nullable 配置
- [x] 不需要在 csproj 中使用条件编译区分不同目标框架的 nullable 配置

### 待确认项

- [ ] 源生成器生成的代码是否需要保留 nullable 指令
  - **建议**：保留，确保生成代码的自包含性和正确性
    - 保留

- [ ] 是否需要为不同目标框架设置不同的 nullable 配置
  - **建议**：不需要，统一使用 `<Nullable>enable</Nullable>` 即可
    - 根据目标框架兼容性来设定

- [ ] **NEW**: 多框架项目配置 `<Nullable>enable</Nullable>` 时的"框架兼容性适配条件"具体实现方式
  - **需要澄清**：具体是什么条件、如何实现

## 实现策略调整

根据新规范"多框架项目配置 `<Nullable>enable</Nullable>` 时需要加上指定框架兼容性适配条件"：

当前理解（需进一步确认）：
1. **Delly.Modeling.csproj** (目标: netstandard2.0;net6.0)
   - 可能需要使用条件编译来为不同框架设置不同的 nullable 配置
   - 或使用 MSBuild 条件属性来控制 nullable 行为

2. **Delly.Modeling.Generator.csproj** (目标: netstandard2.0)
   - 不需要配置 `<Nullable>enable</Nullable>`

**后续行动**：
- 与产品/技术负责人确认"框架兼容性适配条件"的具体实现方式
- 根据确认结果更新实现步骤和 csproj 配置示例
