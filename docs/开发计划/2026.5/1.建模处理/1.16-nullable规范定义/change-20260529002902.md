# 1.16 功能文档变更记录

**生成时间**: 2026-05-29 00:29:02

## 原始文档备份

[备份文件](./backup-20260529002902.rd)

## 变更内容

### 差异点1：规范要求新增内容

**位置**: `1.16-需求补充.md` 第27行

**新增内容**:
```
- .NET Standard 2.0 不需要配置 `<Nullable>enable</Nullable>`
```

**说明**: memory.rd 中没有此内容，这是新增的规范要求。

**影响**:
- 对于纯 .NET Standard 2.0 项目（如 Delly.Modeling.Generator），不需要添加 `<Nullable>enable</Nullable>` 配置
- 对于多目标框架项目（如 Delly.Modeling，目标为 netstandard2.0;net6.0），可以添加 `<Nullable>enable</Nullable>` 配置，编译器在 netstandard2.0 目标下会忽略此配置

---

### 差异点2：待确认清单状态回退

**位置**: `1.16-需求补充.md` 第46-48行 vs `memory.rd` 第42-44行

**变更说明**:
- memory.rd 中"源生成器生成的代码是否需要保留 nullable 指令"已移至"已确认项"
- 需求补充文件中该项仍保留在"待确认项"

**说明**: 这可能是需求补充文件的编辑状态不一致，本次重新生成将保持需求补充文件的状态。

## 变更原因

本次变更基于任务执行过程中新增的需求补充，主要是：
1. 明确 .NET Standard 2.0 项目不需要配置 `<Nullable>enable</Nullable>`
2. 修正了原需求中对所有项目统一添加 nullable 配置的要求

## 可能产生的影响

1. **功能文档更新**: 需要明确区分纯 .NET Standard 2.0 项目和多目标框架项目的 nullable 配置策略
2. **实现步骤调整**: `Delly.Modeling.Generator.csproj` 不需要添加 `<Nullable>enable</Nullable>` 配置
3. **待确认清单**: 保持"源生成器生成的代码是否需要保留 nullable 指令"在待确认状态

## 待确认清单

根据需求补充文件，本次变更后的待确认清单：

### 已确认项

- [x] .NET Standard 2.0 目标框架编译器会忽略 nullable 配置
- [x] 不需要在 csproj 中使用条件编译区分不同目标框架的 nullable 配置

### 待确认项

- [ ] 源生成器生成的代码是否需要保留 nullable 指令
  - **建议**：保留，确保生成代码的自包含性和正确性
    - 保留

- [ ] 是否需要为不同目标框架设置不同的 nullable 配置
  - **建议**：不需要，统一使用 `<Nullable>enable</Nullable>` 即可
    - 根据目标框架兼容性来设定

## 实现策略调整

根据新规范 ".NET Standard 2.0 不需要配置 `<Nullable>enable</Nullable>`"：

1. **Delly.Modeling.csproj** (目标: netstandard2.0;net6.0)
   - 可以添加 `<Nullable>enable</Nullable>` 配置
   - netstandard2.0 目标会忽略此配置
   - net6.0 目标会启用此配置

2. **Delly.Modeling.Generator.csproj** (目标: netstandard2.0)
   - 不需要添加 `<Nullable>enable</Nullable>` 配置
   - 保持项目文件简洁
