# 1.6 建模添加字段

## 任务信息

- **任务号**: 1.6
- **任务名称**: 建模添加字段
- **责任人**: 郑琳
- **任务类型**: 代码修改
- **任务描述**: 在 `IBaseModel` 中添加 `Type Type` 属性

## 功能概述

在 `IBaseModel` 接口中添加类型信息字段，使模型对象能够提供其所关联的类型信息。

## 技术背景

### 现有实现

当前 `IBaseModel` 接口定义如下：

```csharp
namespace Delly.Modeling
{
    /// <summary>
    /// 模型对象接口，提供属性访问能力
    /// </summary>
    public interface IBaseModel
    {
        /// <summary>
        /// 名称
        /// </summary>
        string Name { get; }

        /// <summary>
        /// 类型全名称
        /// </summary>
        string Namespace { get; }

        /// <summary>
        /// 创建模型类型的新实例
        /// </summary>
        /// <param name="args">构造函数参数数组</param>
        /// <returns>模型类型的新实例</returns>
        object CreateInstance(params object[] args);
    }
}
```

### 新增需求

需要在 `IBaseModel` 接口中添加 `Type Type` 属性，以提供类型信息。

## 实现方案

### 1. 修改 IBaseModel 接口

在 `IBaseModel` 接口中添加以下属性：

```csharp
/// <summary>
/// 类型信息
/// </summary>
Type Type { get; }
```

### 2. TODO: 待确认项

1. **AOT 兼容性**：添加 `Type Type` 属性可能与项目 AOT 编译友好性规范存在冲突
   - TODO: 确认是否需要使用 `System.Type` 类型，还是使用字符串形式表示类型
   - 建议：考虑使用 `string TypeFullName` 替代 `Type Type`，保持 AOT 兼容性

2. **属性命名冲突**：`Type` 作为属性名可能与某些场景下的关键字冲突
   - TODO: 确认属性命名是否需要调整
   - 建议：考虑使用 `ModelType` 或 `TypeInfo` 作为属性名

3. **实现类修改范围**：需要确认哪些实现类需要更新
   - TODO: 确认需要更新的实现类清单

## 使用示例

```csharp
// 获取模型的类型信息
var model = Order.GetEntityModel();
Console.WriteLine($"类型: {model.Type.FullName}");
Console.WriteLine($"名称: {model.Name}");
Console.WriteLine($"命名空间: {model.Namespace}");
```

## 实现步骤

### 开发任务清单

- [ ] TODO: 确认 AOT 兼容性方案（使用 `Type` 还是 `string`）
- [ ] TODO: 确认属性命名
- [ ] 查找所有实现 `IBaseModel` 的类
- [ ] 在 `IBaseModel` 接口中添加 `Type Type` 属性
- [ ] 在所有实现类中添加 `Type Type` 属性的实现
- [ ] 添加 XML 文档注释
- [ ] 更新相关单元测试
- [ ] 在 Deme 项目中添加示例代码
- [ ] 验证 AOT 编译通过

## 参考文档

- [IBaseModel.cs](../../../Delly.Modeling/IBaseModel.cs) - 需要修改的接口
- [代码规范.md](../../../docs/开发指南/代码规范.md) - 代码规范参考
- [注意事项.md](../../../docs/开发指南/注意事项.md) - 开发注意事项

## 验收标准

1. `IBaseModel` 接口包含 `Type Type` 属性（或根据 TODO 确认的替代方案）
2. 所有实现 `IBaseModel` 的类都已更新
3. 属性值正确返回对应的类型信息
4. XML 文档注释完整
5. 单元测试通过
6. Deme 项目中的示例能正常运行
7. TODO: 确认 AOT 编译通过（如使用 `System.Type`）