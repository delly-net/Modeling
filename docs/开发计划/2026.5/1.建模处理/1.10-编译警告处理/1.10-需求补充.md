# 1.10 需求补充

## 原始需求

处理所有的编译警告（包括源生成后的代码）。

## 需求补充

请在开发过程中补充以下需求细节：

### 1. _parser 字段用途确认

问题：生成的 `emc__UserEntity` 和 `emc__UserQuery` 类中有未使用的 `_parser` 字段。

- 字段用途：在 `TryParse` 方法中调用
- 处理方式：保留并实现调用
- 理由：原始设计需求，参考 任务号1.8 

### 2. 条件编译范围确认

问题：CS8632 警告涉及多个文件，需要确认条件编译范围。

- 是否为所有源生成器文件添加 `#nullable enable`？[ ] 是  [X] 否
- 是否为所有 Models/*.cs 文件添加 `#nullable enable`？[X] 是  [ ] 否
- 理由：源生成器项目不支持nullable，只需要在生成的代码中加入 `#nullable enable`，Models/*.cs 中的文件均需要支持object?

### 3. 空引用修复策略确认

问题：生成代码中的空引用警告有多种修复方式。

- CS8600 修复方式：[X] `as` 转换 + null 检查  [ ] 其他：________
- CS8602 修复方式：[X] null 检查  [ ] 其他：________
- CS8601 修复方式：[X] 使用非可空类型  [ ] 其他：________
- 理由：________________________

### 4. XML 注释详细程度确认

问题：生成的 XML 注释详细程度影响代码体积。

- 是否添加 `<param>` 详细描述？[X] 是  [ ] 否
- 是否添加 `<returns>` 详细描述？[X] 是  [ ] 否
- 是否添加 `<summary>` 之外的注释？[X] 是  [ ] 否
- 理由：________________________

### 5. 其他补充需求

如有其他需求补充，请在此记录：

____________________________________________________________
____________________________________________________________