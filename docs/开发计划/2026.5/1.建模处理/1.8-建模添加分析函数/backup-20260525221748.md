# 1.8 建模添加分析函数

## 任务信息

- **任务号**: 1.8
- **任务名称**: 建模添加分析函数
- **责任人**: 郑琳
- **任务类型**: 代码修改
- **任务描述**: 在 `IBaseModel` 中添加 `object Parse(object obj)` 与 `object TryParse(object obj)` 函数

## 功能概述

在 `IBaseModel` 接口中添加 `Parse` 和 `TryParse` 函数，提供对象值解析能力。同时新增 `IParsable<T>` 泛型接口，类型可通过实现该接口提供自定义解析逻辑。源生成器在生成解析逻辑时会查找 `IParsable<T>` 的实现类，实例化对象并调用实例方法。`Parse` 函数用于将输入对象解析为目标类型实例，解析失败时抛出异常；`TryParse` 函数提供安全的解析方式，解析失败时返回 `null`。

**重要约束**：
- 所有 `interface` 定义都不允许使用 `static abstract`
- 本项目不允许出现反射方式调用！！！
- 接口定义使用非空类型 `object`，实现类内部可返回 `null`
- `Parse` 函数解析失败时应抛出异常
- `TryParse` 函数解析失败时应返回 `null`，不应抛出异常
- 源生成器生成的 `Parse` 函数应先调用 `TryParse`，再判断结果
- 源生成器生成的 `TryParse` 函数应查找 `IParsable<T>` 的实现类并实例化调用
- 没有实现 `IParsable<T>` 时，`TryParse` 应抛出 `NotSupportedException`
- 不同 .NET 版本使用条件编译提供支持
- `IParsable<T>` 实现类必须提供无参构造函数

## 技术背景

### 现有实现

当前 `IBaseModel` 接口定义如下：

```csharp
#if !NETSTANDARD2_0
#nullable enable
#endif

using System;

namespace Delly.Modeling
{
    /// <summary>
    /// 模型对象接口，提供属性访问能力
    /// </summary>
    public interface IBaseModel
    {
        /// <summary>
        /// 名称
        /// </summary>
        string Name { get; }

        /// <summary>
        /// 类型全名称
        /// </summary>
        string Namespace { get; }

        /// <summary>
        /// 创建模型类型的新实例
        /// </summary>
        /// <param name="args">构造函数参数数组</param>
        /// <returns>模型类型的新实例</returns>
        object CreateInstance(params object[] args);

        /// <summary>
        /// 模型类型信息，源生成阶段使用 typeof(T) 赋值
        /// </summary>
        Type ClassType { get; }
    }
}
```

现有的基础 Model 对象（如 `StringModel`、`Int32Model`）手动实现了 `IBaseModel` 接口：

```csharp
public sealed class StringModel : IBaseModel
{
    // 固定静态共享实例
    private static readonly StringModel _instance = new StringModel();

    public static StringModel Instance => _instance;

    public string Name => nameof(String);
    public string Namespace => nameof(System);
    public Type ClassType => typeof(string);
    public object CreateInstance(params object[] args) => args == null || args.Length == 0 ? string.Empty : (args[0] as string) ?? string.Empty;
}
```

### 新增需求

1. **新增 IParsable<T> 泛型接口**：定义 `TryParse` 方法，类型可通过实现该接口提供解析逻辑
2. **在 IBaseModel 中添加解析函数**：`Parse` 和 `TryParse`
3. **源生成逻辑更新**：查找 `IParsable<T>` 的实现类，实例化对象并调用实例方法
4. **基础 Model 对象更新**：添加独立的 `Parse` 和 `TryParse` 函数

## 实现方案

### 1. 新增 IParsable<T> 接口

```csharp
#if !NETSTANDARD2_0
#nullable enable
#endif

namespace Delly.Modeling
{
    /// <summary>
    /// 可分析类型接口，用于提供类型解析能力
    /// </summary>
    /// <typeparam name="T">目标类型</typeparam>
    /// <remarks>
    /// 实现此接口的类型为 T 提供解析能力。源生成器会查找 IParsable&lt;T&gt; 的实现类，
    /// 通过无参构造函数实例化对象，并调用 TryParse 方法进行解析。
    /// 实现类必须提供无参构造函数。
    /// </remarks>
    public interface IParsable<T>
    {
        /// <summary>
        /// 尝试将输入对象解析为目标类型实例
        /// </summary>
        /// <param name="obj">输入对象</param>
        /// <returns>目标类型实例，解析失败时返回 null</returns>
#if NETSTANDARD2_0
        object TryParse(object obj);
#else
        object? TryParse(object? obj);
#endif
    }
}
```

### 2. 修改 IBaseModel 接口

**注意**：接口定义使用非空类型 `object`，通过条件编译支持不同 .NET 版本。

```csharp
/// <summary>
/// 将输入对象解析为目标类型实例
/// </summary>
/// <param name="obj">输入对象</param>
/// <returns>目标类型实例</returns>
/// <exception cref="ArgumentException">当对象无法转换为目标类型时抛出</exception>
#if NETSTANDARD2_0
object Parse(object obj);
#else
object Parse(object? obj);
#endif

/// <summary>
/// 尝试将输入对象解析为目标类型实例
/// </summary>
/// <param name="obj">输入对象</param>
/// <returns>目标类型实例，解析失败时返回 null</returns>
#if NETSTANDARD2_0
object TryParse(object obj);
#else
object? TryParse(object? obj);
#endif
```

### 3. 源生成器实现

需要在以下源生成器中添加 `Parse` 和 `TryParse` 函数的生成代码：

| 源生成器 | 特性 | 生成的类 |
|:--------|:----|:--------|
| `ModelableSourceGenerator` | `ModelableAttribute` | `emc__{ClassName}` |
| `ModelTableSourceGenerator` | `MoTableAttribute` | `emc__{ClassName}` |
| `ModelTableSourceGenerator` | `MoQueryAttribute` | `emc__{ClassName}` |

**生成的 `TryParse` 函数逻辑**（非 .NET Standard 2.0）：

```csharp
/// <summary>
/// 尝试将输入对象解析为目标类型实例
/// </summary>
/// <param name="obj">输入对象</param>
/// <returns>目标类型实例，解析失败时返回 null</returns>
public override object? TryParse(object? obj)
{
    if (obj == null)
        return null;

    if (obj is {ClassName} target)
        return target;

    // 查找 IParsable<{ClassName}> 的实现类
    // 命名约定：{ClassName}Parser
    var parser = new {ClassName}Parser();
    var result = parser.TryParse(obj);
    return result;
}
```

**生成的 `TryParse` 函数逻辑**（.NET Standard 2.0）：

```csharp
/// <summary>
/// 尝试将输入对象解析为目标类型实例
/// </summary>
/// <param name="obj">输入对象</param>
/// <returns>目标类型实例，解析失败时返回 null</returns>
public override object TryParse(object obj)
{
    if (obj == null)
        return null;

    if (obj is {ClassName} target)
        return target;

    // 查找 IParsable<{ClassName}> 的实现类
    // 命名约定：{ClassName}Parser
    var parser = new {ClassName}Parser();
    var result = parser.TryParse(obj);
    return result;
}
```

**生成的 `Parse` 函数逻辑**（非 .NET Standard 2.0）：

```csharp
/// <summary>
/// 将输入对象解析为目标类型实例
/// </summary>
/// <param name="obj">输入对象</param>
/// <returns>目标类型实例</returns>
/// <exception cref="ArgumentException">当对象无法转换为目标类型时抛出</exception>
public override object Parse(object? obj)
{
    var result = TryParse(obj);
    if (result == null)
        throw new ArgumentException($"Cannot convert {obj?.GetType().Name ?? "null"} to {ClassName}");
    return result;
}
```

**生成的 `Parse` 函数逻辑**（.NET Standard 2.0）：

```csharp
/// <summary>
/// 将输入对象解析为目标类型实例
/// </summary>
/// <param name="obj">输入对象</param>
/// <returns>目标类型实例</returns>
/// <exception cref="ArgumentException">当对象无法转换为目标类型时抛出</exception>
public override object Parse(object obj)
{
    var result = TryParse(obj);
    if (result == null)
        throw new ArgumentException($"Cannot convert {(obj != null ? obj.GetType().Name : "null")} to {ClassName}");
    return result;
}
```

### 4. 基础 Model 对象更新

为所有基础 Model 对象（`StringModel`、`Int32Model`、`Int64Model`、`BooleanModel`、`DecimalModel`、`DoubleModel`、`DateTimeModel`、`GuidModel`）添加 `Parse` 和 `TryParse` 函数：

```csharp
public sealed class Int32Model : IBaseModel
{
    // ... 现有代码 ...

    /// <summary>
    /// 将输入对象解析为 Int32 实例
    /// </summary>
    /// <param name="obj">输入对象</param>
    /// <returns>Int32 实例</returns>
    /// <exception cref="ArgumentException">当对象无法转换为 Int32 时抛出</exception>
#if NETSTANDARD2_0
    public object Parse(object obj)
#else
    public object Parse(object? obj)
#endif
    {
        var result = TryParse(obj);
        if (result == null)
            throw new ArgumentException($"Cannot convert {(obj != null ? obj.GetType().Name : "null")} to Int32");
        return result;
    }

    /// <summary>
    /// 尝试将输入对象解析为 Int32 实例
    /// </summary>
    /// <param name="obj">输入对象</param>
    /// <returns>Int32 实例，解析失败时返回 null</returns>
#if NETSTANDARD2_0
    public object TryParse(object obj)
#else
    public object? TryParse(object? obj)
#endif
    {
        if (obj == null)
            return null;

        if (obj is int value)
            return value;

        if (obj is string str && int.TryParse(str, out var intVal))
            return intVal;

        if (obj is long longVal && longVal >= int.MinValue && longVal <= int.MaxValue)
            return (int)longVal;

        return null;
    }
}
```

### 5. 支持的解析场景

**基础类型解析**（由基础 Model 对象处理）：

| 目标类型 | 支持的源类型 | 处理方式 |
|:--------|:------------|:--------|
| int | int | 直接返回 |
| int | string | 使用 int.TryParse |
| int | long | 范围检查后转换 |
| string | string | 直接返回 |
| bool | bool | 直接返回 |
| bool | string | 使用 bool.TryParse |
| long | long | 直接返回 |
| long | string | 使用 long.TryParse |
| long | int | 直接转换 |
| decimal | decimal | 直接返回 |
| decimal | string | 使用 decimal.TryParse |
| decimal | int | 直接转换 |
| decimal | double | 直接转换 |
| double | double | 直接返回 |
| double | string | 使用 double.TryParse |
| double | decimal | 直接转换 |
| DateTime | DateTime | 直接返回 |
| DateTime | string | 使用 DateTime.TryParse |
| Guid | Guid | 直接返回 |
| Guid | string | 使用 Guid.TryParse |

**自定义类型解析**（由 IParsable<T> 接口处理）：
- 创建 `IParsable<T>` 的实现类
- 实现类命名约定：`{ClassName}Parser`
- 实现类提供无参构造函数
- 源生成器实例化对象并调用实例方法

### 6. 用户使用示例

```csharp
// 使用基础 Model 对象的解析函数
var intModel = Int32Model.Instance;

// Parse - 解析成功
int value1 = (int)intModel.Parse("123");
Console.WriteLine(value1);  // 输出: 123

// Parse - 解析失败抛出异常
try
{
    intModel.Parse("invalid");
}
catch (ArgumentException ex)
{
    Console.WriteLine(ex.Message);  // 输出: Cannot convert String to Int32
}

// TryParse - 解析成功
object result1 = intModel.TryParse("456");
if (result1 != null)
{
    Console.WriteLine(result1);  // 输出: 456
}

// TryParse - 解析失败返回 null
object result2 = intModel.TryParse("invalid");
Console.WriteLine(result2);  // 输出: (空/无值)

// 自定义类型解析
[Modelable]
public partial class Order
{
    public long Id { get; set; }
    public string Name { get; set; }
}

// 创建 IParsable<Order> 的实现类
public class OrderParser : IParsable<Order>
{
    public object TryParse(object obj)
    {
        if (obj == null)
            return null;

        if (obj is Order order)
            return order;

        if (obj is long id)
            return new Order { Id = id, Name = string.Empty };

        if (obj is string str && long.TryParse(str, out var longId))
            return new Order { Id = longId, Name = string.Empty };

        return null;
    }
}

var orderModel = Order.GetEntityModel();
var order = (Order)orderModel.Parse(123L);
Console.WriteLine(order.Id);  // 输出: 123

var order2 = (Order)orderModel.TryParse("456");
Console.WriteLine(order2?.Id);  // 输出: 456
```

## 实现步骤

### 开发任务清单

- [ ] 创建 `IParsable<T>` 接口定义（泛型接口，定义 TryParse 方法）
- [ ] 在 `IBaseModel` 接口中添加 `Parse` 函数定义（使用非空类型和条件编译）
- [ ] 在 `IBaseModel` 接口中添加 `TryParse` 函数定义（使用非空类型和条件编译）
- [ ] 在 `ModelableSourceGenerator` 中添加 `TryParse` 函数生成逻辑
  - [ ] 生成 `new {ClassName}Parser()` 实例化代码
  - [ ] 调用实例对象的 TryParse 方法
  - [ ] 处理解析结果为 null 的情况
- [ ] 在 `ModelableSourceGenerator` 中添加 `Parse` 函数生成逻辑
  - [ ] 实现先调用 TryParse 再判断的逻辑
  - [ ] 实现为空时抛出 ArgumentException 的逻辑
  - [ ] 使用条件编译支持不同 .NET 版本
- [ ] 在 `ModelTableSourceGenerator` 中为 `MoTable` 特性添加相同逻辑
- [ ] 在 `ModelTableSourceGenerator` 中为 `MoQuery` 特性添加相同逻辑
- [ ] 为 `StringModel` 添加 `Parse` 和 `TryParse` 函数
- [ ] 为 `Int32Model` 添加 `Parse` 和 `TryParse` 函数
- [ ] 为 `Int64Model` 添加 `Parse` 和 `TryParse` 函数
- [ ] 为 `BooleanModel` 添加 `Parse` 和 `TryParse` 函数
- [ ] 为 `DecimalModel` 添加 `Parse` 和 `TryParse` 函数
- [ ] 为 `DoubleModel` 添加 `Parse` 和 `TryParse` 函数
- [ ] 为 `DateTimeModel` 添加 `Parse` 和 `TryParse` 函数
- [ ] 为 `GuidModel` 添加 `Parse` 和 `TryParse` 函数
- [ ] 添加 XML 文档注释
- [ ] 更新相关单元测试
- [ ] 在 Deme 项目中添加示例代码
- [ ] 验证 AOT 编译通过
- [ ] 使用 `PublishAot` 验证发布后正常运行
- [ ] 验证 .NET Standard 2.0 兼容性
- [ ] 验证 .NET 6.0 兼容性

## TODO: 待确认项

1. **IParsable<T> 实现类命名约定**：
   - 当前方案：`{ClassName}Parser`
   - 是否需要支持自定义实现类名？

2. **多个实现类处理**：
   - 如果有多个类实现了 `IParsable<{ClassName}>`，如何选择？
   - 当前方案：默认使用 `{ClassName}Parser`
   - 是否需要支持配置？

3. **实例对象缓存**：
   - 每次解析都实例化新对象
   - 是否需要缓存实例对象以减少分配？

## 参考文档

- [IBaseModel.cs](../../../Delly.Modeling/IBaseModel.cs) - 需要修改的接口
- [IParsable.cs](../../../Delly.Modeling/IParsable.cs) - 需要新增的接口
- [ModelableSourceGenerator.cs](../../../Delly.Modeling.Generator/ModelableSourceGenerator.cs) - 模型源生成器
- [ModelTableSourceGenerator.cs](../../../Delly.Modeling.Generator/ModelTableSourceGenerator.cs) - 实体源生成器
- [StringModel.cs](../../../Delly.Modeling/Models/StringModel.cs) - String 建模示例
- [Int32Model.cs](../../../Delly.Modeling/Models/Int32Model.cs) - Int32 建模示例
- [代码规范.md](../../../docs/开发指南/代码规范.md) - 代码规范参考
- [注意事项.md](../../../docs/开发指南/注意事项.md) - 开发注意事项
- [需求补充.md](./1.8-需求补充.md) - 需求补充文档
- [backup-20260525220318.md](./backup-20260525220318.md) - 原始功能文档备份
- [change-20260525220318.md](./change-20260525220318.md) - 变更记录

## 验收标准

1. `IParsable<T>` 接口为泛型接口，包含 `TryParse` 方法定义
2. `IBaseModel` 接口包含 `Parse` 函数定义（使用非空类型和条件编译）
3. `IBaseModel` 接口包含 `TryParse` 函数定义（使用非空类型和条件编译）
4. `ModelableSourceGenerator` 生成的 `TryParse` 函数通过 `new {ClassName}Parser()` 实例化对象
5. `ModelableSourceGenerator` 生成的 `TryParse` 函数调用实例对象的 TryParse 方法
6. `ModelableSourceGenerator` 生成的 `Parse` 函数先调用 TryParse 再判断结果
7. `ModelableSourceGenerator` 生成的代码使用条件编译支持不同 .NET 版本
8. `ModelTableSourceGenerator` 为 `MoTable` 特性生成的代码包含相同逻辑
9. `ModelTableSourceGenerator` 为 `MoQuery` 特性生成的代码包含相同逻辑
10. 所有基础 Model 对象都已添加 `Parse` 和 `TryParse` 函数
11. `Parse` 函数解析失败时抛出 `ArgumentException`
12. `TryParse` 函数解析失败时返回 `null`
13. 源生成器代码中不包含任何反射调用（`GetMethod`, `Invoke` 等）
14. `IParsable<T>` 实现类通过无参构造函数实例化
15. XML 文档注释完整
16. 单元测试通过
17. Deme 项目中的示例能正常运行
18. AOT 编译通过
19. `PublishAot` 发布后正常运行
20. .NET Standard 2.0 兼容性验证通过
21. .NET 6.0 兼容性验证通过