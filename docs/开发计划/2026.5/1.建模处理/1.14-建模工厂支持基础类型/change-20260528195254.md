# 1.14 变更记录

## 变更时间

2026-05-28 19:52:54

## 原始功能文档备份

[backup-20260528195254.rd](./backup-20260528195254.rd)

## 变更内容

### 1. 存储方案确定

**原文档建议**: 方案 A（独立字典 `_baseModels`）

**用户选择**: 方案 B（统一字典 `_models`）

**变更原因**: 用户需求

**实现方式**:
- 将 `_models` 字段类型从 `Dictionary<string, IEntityModel>` 改为 `Dictionary<string, IBaseModel>`
- 统一存储实体模型和基础类型建模

### 2. 查询方法确定

**原文档建议**: 选项 1（新增 `GetBaseModel` 方法）

**用户选择**: 选项 2（统一使用 `GetModel` 方法）

**变更原因**: 用户需求

**实现方式**:
- 不新增 `GetBaseModel` 方法
- 统一使用 `GetModel` 方法查询所有类型建模

### 3. StringModel 键值确定

**原文档建议**: 选项 2（同时支持两个键）

**用户选择**: 选项 2（同时支持两个键）

**状态**: 无变更

**实现方式**:
- 同时支持 `string.Empty` 和 `System.String` 两个键

### 4. 泛型方法支持

**原文档建议**: 使用 `typeof + switch case` 模式匹配

**用户选择**: 使用 `typeof + switch case` 模式匹配

**状态**: 无变更

**实现方式**:
- 在 `GetModel<T>()` 方法中添加基础类型的 `typeof + switch case` 支持

### 5. 开发中确认项

**新增确认结果**:
- 是否需要添加 `HasBaseModel` 方法？不需要
- 是否需要添加 `GetAllBaseModels` 方法？不需要
- 是否需要更新 `Count` 属性统计基础类型建模数量？不需要

## 可能产生的影响

1. **类型安全**: 由于使用统一字典 `Dictionary<string, IBaseModel>`，查询时需要进行类型判断
2. **代码修改**: 需要修改 `DefaultEntityModelFactory` 类的字段定义和构造函数
3. **泛型方法**: 需要扩展 `GetModel<T>()` 方法支持基础类型

## 变更后 TODO 清单

- [x] 存储方案确认（统一字典 `_models`）
- [x] 查询方法确认（统一使用 `GetModel`）
- [x] StringModel 键值确认（同时支持两个键）
- [x] 泛型方法支持确认（typeof + switch case）
- [ ] 在 `DefaultEntityModelFactory` 中修改 `_models` 字段类型
- [ ] 实现 `RegisterBaseTypeModels` 私有方法
- [ ] 在构造函数中调用 `RegisterBaseTypeModels`
- [ ] 扩展 `GetModel<T>()` 方法支持基础类型
- [ ] 更新 XML 文档注释
- [ ] 在 Deme 项目中添加示例代码
- [ ] 更新单元测试
- [ ] 验证所有基础类型建模正确注册
- [ ] 验证查询方法返回正确结果
