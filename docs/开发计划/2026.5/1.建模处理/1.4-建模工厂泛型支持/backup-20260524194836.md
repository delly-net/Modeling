# 1.4 建模工厂泛型支持

## 任务信息
- **任务号**: 1.4
- **任务名称**: 建模工厂泛型支持
- **责任人**: 郑琳
- **任务类型**: 功能开发
- **任务描述**: 在建模工厂中添加根据泛型获取建模能力

## 功能概述

在建模工厂（`IEntityModelFactory` / `DefaultEntityModelFactory`）中添加泛型方法支持，允许通过类型参数直接获取对应类型的实体模型，提供类型安全的 API，提升开发体验。

**重要约束**：
- 必须保持与现有接口的向后兼容性
- 泛型实现应避免运行时反射，确保 AOT 编译友好
- 使用 `typeof` 和 `switch case` 实现编译时类型判断
- 泛型方法仅提供以下两个：`GetModel<T>()` 和 `TryGetModel<T>()`

## 技术背景

### 现有实现

**现有工厂接口**：
```csharp
/// <summary>
/// 实体建模工厂接口，定义实体建模管理的标准
/// </summary>
public interface IEntityModelFactory
{
    /// <summary>
    /// 获取指定类全称的实体模型
    /// </summary>
    /// <param name="fullName">类全称，格式为 {Namespace}.{ClassName}</param>
    /// <returns>实体模型实例，如不存在则返回 null</returns>
    IEntityModel? GetModel(string fullName);

    /// <summary>
    /// 获取指定表名的实体模型
    /// </summary>
    /// <param name="tableName">表名/查询对象名称</param>
    /// <returns>实体模型实例，如不存在则返回 null</returns>
    IEntityModel? GetModelByTableName(string tableName);

    /// <summary>
    /// 获取所有实体模型
    /// </summary>
    /// <returns>所有已注册的实体模型</returns>
    IEnumerable<IEntityModel> GetAllModels();

    /// <summary>
    /// 获取指定命名空间的所有实体模型
    /// </summary>
    /// <param name="ns">命名空间</param>
    /// <returns>指定命名空间的所有实体模型</returns>
    IEnumerable<IEntityModel> GetModelsByNamespace(string ns);

    /// <summary>
    /// 检查是否存在指定类全称的实体模型
    /// </summary>
    /// <param name="fullName">类全称</param>
    /// <returns>是否存在</returns>
    bool HasModel(string fullName);

    /// <summary>
    /// 获取模型总数
    /// </summary>
    int Count { get; }

    /// <summary>
    /// 添加建模集合
    /// </summary>
    /// <param name="modelSet">建模集合</param>
    void AddSet(IEntityModelSet modelSet);

    /// <summary>
    /// 添加单个实体模型
    /// </summary>
    /// <param name="model">实体模型</param>
    void Add(IEntityModel model);
}
```

**现有使用方式**：
```csharp
var factory = new DefaultEntityModelFactory();
factory.Add(User.GetEntityModel());
factory.Add(Order.GetEntityModel());

// 通过类全称获取
var userModel = factory.GetModel("MyApp.Models.User");

// 通过表名获取
var orderModel = factory.GetModelByTableName("orders");
```

### 新增需求

为建模工厂添加泛型方法支持，提供类型安全的 API：

```csharp
/// <summary>
/// 获取指定类型的实体模型
/// </summary>
/// <typeparam name="T">实体类型</typeparam>
/// <returns>指定类型的实体模型</returns>
/// <exception cref="NotSupportedException">当类型未注册时抛出</exception>
IEntityModel GetModel<T>() where T : class;

/// <summary>
/// 尝试获取指定类型的实体模型
/// </summary>
/// <typeparam name="T">实体类型</typeparam>
/// <returns>指定类型的实体模型，未找到时返回 null</returns>
IEntityModel? TryGetModel<T>() where T : class;
```

**TODO: 待确认项**：
- TODO: 确认泛型约束是否需要更严格（建议：保持 `where T : class`，不添加其他约束）
- TODO: 确认异常类型（建议：使用 `NotSupportedException`）
- TODO: 确认是否需要通过特性标记哪些类型需要注册（建议：否，通过调用 `Add` 方法动态注册时收集类型信息）

### 现有接口

相关实体模型接口：
```csharp
/// <summary>
/// 实体模型接口
/// </summary>
public interface IEntityModel
{
    /// <summary>
    /// 表名/查询对象名称
    /// </summary>
    string Name { get; }

    /// <summary>
    /// 类名
    /// </summary>
    string ClassName { get; }

    /// <summary>
    /// 命名空间
    /// </summary>
    string Namespace { get; }

    /// <summary>
    /// 获取属性集合
    /// </summary>
    IEntityModelProperty[] GetProperties();

    /// <summary>
    /// 获取索引集合
    /// </summary>
    EntityModelIndex[] GetIndexes();

    /// <summary>
    /// 获取属性
    /// </summary>
    IEntityModelProperty? GetProperty(string name);
}
```

## 实现方案

### 1. 具体步骤

1. **更新 IEntityModelFactory 接口**
   - 添加 `GetModel<T>()` 方法定义
   - 添加 `TryGetModel<T>()` 方法定义
   - 处理不同 .NET 版本的 nullable 兼容性

2. **更新 DefaultEntityModelFactory 实现**
   - 添加 `Dictionary<Type, string>` 用于存储类型到类全称的映射
   - 在 `Add(IEntityModel model)` 方法中收集类型信息
   - 在 `AddSet(IEntityModelSet modelSet)` 方法中收集类型信息
   - 实现 `GetModel<T>()` 方法，使用 `switch case` 匹配类型
   - 实现 `TryGetModel<T>()` 方法，返回 null 或模型对象

3. **添加单元测试**
   - 测试 `GetModel<T>()` 正确返回指定类型
   - 测试 `GetModel<T>()` 对未注册类型抛出异常
   - 测试 `TryGetModel<T>()` 返回正确值或 null
   - 测试通过 `Add` 方法注册的类型可被泛型方法获取
   - 测试通过 `AddSet` 方法注册的类型可被泛型方法获取

4. **更新文档**
   - 更新用户指南，说明泛型用法

### 2. TODO: 待确认项

- TODO: 确认类型收集方式（建议：在 `Add` 和 `AddSet` 方法调用时，通过调用栈分析获取 Type 信息，或要求用户额外调用泛型注册方法）
- TODO: 确认是否需要提供泛型版本的 `Add` 方法（建议：考虑添加 `Add<T>(IEntityModel model)` 方法）
- TODO: 确认类型映射的生命周期管理（建议：与模型字典同步管理）

## 使用示例

### 示例 1：泛型方式获取模型

```csharp
var factory = new DefaultEntityModelFactory();

// 注册模型
factory.Add(User.GetEntityModel());
factory.Add(Order.GetEntityModel());

// 通过泛型获取模型
var userModel = factory.GetModel<User>();
Console.WriteLine(userModel.Name);

// 使用 TryGetModel 避免异常
var productModel = factory.TryGetModel<Product>();
if (productModel != null)
{
    Console.WriteLine(productModel.Name);
}
```

### 示例 2：与 MoSet 配合使用

```csharp
[MoSet]
public partial class EntityModelSet;

var factory = new DefaultEntityModelFactory();
var entitySet = new EntityModelSet();

// 通过建模集合注册
factory.AddSet(entitySet);

// 通过泛型获取
var userModel = factory.GetModel<User>();
var orderModel = factory.GetModel<Order>();
```

### 示例 3：类型收集方案（待确认）

**方案 A：调用时动态收集**
```csharp
// 用户需要显式调用注册方法
var factory = new DefaultEntityModelFactory();

// 注册时提供类型信息
factory.RegisterType<User>();
factory.Add(User.GetEntityModel());

// 泛型获取
var userModel = factory.GetModel<User>();
```

**方案 B：泛型 Add 方法**
```csharp
var factory = new DefaultEntityModelFactory();

// 使用泛型 Add 方法，自动收集类型
factory.Add<User>(User.GetEntityModel());

// 泛型获取
var userModel = factory.GetModel<User>();
```

**TODO: 确认采用哪种方案**

## 实现步骤

### 开发任务清单

- [ ] 更新 `IEntityModelFactory` 接口
  - [ ] 添加泛型方法定义
  - [ ] 处理不同 .NET 版本的 nullable 兼容性
- [ ] 更新 `DefaultEntityModelFactory` 实现
  - [ ] 添加类型映射字典
  - [ ] 在 `Add` 方法中收集类型信息
  - [ ] 在 `AddSet` 方法中收集类型信息
  - [ ] 实现 `GetModel<T>()` 方法
  - [ ] 实现 `TryGetModel<T>()` 方法
- [ ] 编写单元测试
  - [ ] 测试泛型获取功能
  - [ ] 测试异常处理
  - [ ] 测试类型收集功能
- [ ] 更新文档
  - [ ] 更新用户指南
  - [ ] 添加代码示例

## 参考文档

- [IEntityModelFactory.cs](../../Delly.Modeling/IEntityModelFactory.cs) - 建模工厂接口
- [DefaultEntityModelFactory.cs](../../Delly.Modeling/DefaultEntityModelFactory.cs) - 建模工厂实现
- [1.3-建模集泛型支持.md](./1.3-建模集泛型支持/1.3-建模集泛型支持.md) - 参考泛型实现方式

## 验收标准

- `GetModel<T>()` 能正确返回指定类型的模型
- `GetModel<T>()` 对未注册类型抛出 `NotSupportedException`
- `TryGetModel<T>()` 能正确返回模型对象或 null
- 类型收集机制正常工作
- 单元测试覆盖率 ≥ 80%
- 文档更新完成，示例代码可运行
- 通过 AOT 编译测试