# 2026.5 迭代版本 功能清单

## 1.建模处理

| 任务号 | 任务名称 | 责任人 | 任务类型 | 任务描述 |
|:----:|:----:|:----:|:----:|:----|
| 1.1 | 查询对象建模支持 | 郑琳 | 功能开发 | 添加针对查询对象的建模源生成支持 |
| 1.2 | 建模工厂支持 | 郑琳 | 功能开发 | 添加针对建模同一管理的建模工厂能力支持 |
| 1.3 | 建模集泛型支持 | 郑琳 | 功能开发 | 在建模集源生成中添加泛型转换支持 |
| 1.4 | 建模工厂泛型支持 | 郑琳 | 功能开发 | 在建模工厂中添加根据泛型获取建模能力 |
| 1.5 | 建模创建对象 | 郑琳 | 功能开发 | 添加建模直接创建对象实例的能力 |
| 1.6 | 建模添加字段 | 郑琳 | 代码修改 | 在 `IBaseModel` 中添加 `Type Type` 属性 |
| 1.7 | 建模属性添加PropertyType字段 | 郑琳 | 代码修改 | 在 `IModelProperty` 中添加 `Type PropertyType` 属性 |
| 1.8 | 建模添加分析函数 | 郑琳 | 代码修改 | 在 `IBaseModel` 中添加 `object Parse(object? obj)` 与 `object? TryParse(object? obj)` 函数 |
| 1.9 | 建模可分析特性修改 | 郑琳 | 需求变更 | 修改 `ParsableAttribute` 实现在目标类上通过定义 `[Parsable(typeof(UserParser))]` 方式实现对 `IParsable<T>` 实现类的源生成支持 |
| 1.10 | 编译警告处理 | 郑琳 | 代码优化 | 处理所有的编译警告（包括源生成后的代码） |
| 1.11 | 建模属性添加读写标记 | 郑琳 | 代码修改 | 在 `IModelProperty` 中添加 `bool CanRead` 和 `bool CanWrite` 属性 |
| 1.12 | 建模对象添加属性 | 郑琳 | 代码修改 | 在 `IBaseModel` 中添加 `bool IsValue` 属性，基础值对象为true（含string），一般class对象为false |
| 1.13 | Demo代码整理 | 郑琳 | 代码优化 | 将Demo项目中 `Program.cs` 中的代码，按照功能类型，参考 `CreateInstanceDemo.cs`，进行单独class封装后调用 |
| 1.14 | 建模工厂支持基础类型 | 郑琳 | 代码优化 | `DefaultEntityModelFactory` 类中初始化时添加所有基础建模 |
| 1.15 | 代码规范整理 | 郑琳 | 代码优化 | 根据 `/docs/开发指南/代码规范.md` 中的规范进行全部代码文件优化 |